import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.client-sales-r',
  appName: 'client-sales-r',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
